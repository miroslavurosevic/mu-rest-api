# REST API

Simple REST API with two endpoints:

- /getMovie : accepts three parameters, title (required), year (optional), and plotType (optional)


- /getBook: requires title parameter

The response returns movie or book infromation in JSON format.